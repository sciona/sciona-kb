---
fqdn: sciona.atoms.signal_processing.e2e_ppg.reconstruction.align_and_stitch_signals_by_peak_boundaries
predicate_id: align_and_stitch_signals_by_peak_boundaries
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(ppg_clean_norm: np.ndarray, noise_candidate: np.ndarray, sampling_rate_state: int, upsampling_rate_state: float|int) -> tuple[list[float], int]"
informal_desc: Upsample clean and generated signals, detect peaks, and stitch them at physiologic boundary points.
conceptual_summary: ""
inputs:
  - name: ppg_clean_norm
    type_desc: np.ndarray
    constraints: scaled clean context
  - name: noise_candidate
    type_desc: np.ndarray
    constraints: generated patch
  - name: sampling_rate_state
    type_desc: int
    constraints: "> 0"
  - name: upsampling_rate_state
    type_desc: float
    constraints: "> 0"
outputs:
  - name: stitched_signal_upsampled
    type_desc: list[float]
    constraints: clean prefix + generated suffix in upsampled domain
  - name: stitched_native_len
    type_desc: int
    constraints: len(ppg_clean_norm) + len(noise_candidate)
concept_type: searching
verification_level: type_checked
used_by_cdgs: []
references:
  - title: wang2022ppg
    citation: Exact E2E-PPG reconstruction attribution.
  - title: feli2023pipeline
    citation: Primary E2E-PPG pipeline paper.
  - title: repo_e2e_ppg
    citation: Upstream implementation is mapped to the vendored E2E-PPG repository.
title: Align_and_stitch_signals_by_peak_boundaries
description: Upsample clean and generated signals, detect peaks, and stitch them at physiologic boundary points.
---

# Align_and_stitch_signals_by_peak_boundaries

> Upsample clean and generated signals, detect peaks, and stitch them at physiologic boundary points.

## Conceptual Summary



## Type Signature

```
(ppg_clean_norm: np.ndarray, noise_candidate: np.ndarray, sampling_rate_state: int, upsampling_rate_state: float|int) -> tuple[list[float], int]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
