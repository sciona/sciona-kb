---
fqdn: sciona.atoms.signal_processing.e2e_ppg.reconstruction.identify_eligible_noise_windows
predicate_id: identify_eligible_noise_windows
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(noisy_indices_grouped: list[list[int]], clean_indices_flat: list[int], max_rec_length: int) -> list[dict]"
informal_desc: Select noisy segments that are short enough and have a fully clean preceding context window.
conceptual_summary: ""
inputs:
  - name: noisy_indices_grouped
    type_desc: list[list[int]]
    constraints: Grouped noisy segments at working rate
  - name: clean_indices_flat
    type_desc: list[int]
    constraints: Flattened clean indices
  - name: max_rec_length
    type_desc: int
    constraints: Context and noisy-length limit
outputs:
  - name: candidate_windows
    type_desc: list[dict]
    constraints: Each item includes noise indices and reconstruction context bounds
concept_type: searching
verification_level: type_checked
used_by_cdgs: []
references:
  - title: wang2022ppg
    citation: Exact E2E-PPG reconstruction attribution.
  - title: feli2023pipeline
    citation: Primary E2E-PPG pipeline paper.
  - title: repo_e2e_ppg
    citation: Upstream implementation is mapped to the vendored E2E-PPG repository.
title: Identify_eligible_noise_windows
description: Select noisy segments that are short enough and have a fully clean preceding context window.
---

# Identify_eligible_noise_windows

> Select noisy segments that are short enough and have a fully clean preceding context window.

## Conceptual Summary



## Type Signature

```
(noisy_indices_grouped: list[list[int]], clean_indices_flat: list[int], max_rec_length: int) -> list[dict]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
