---
fqdn: sciona.atoms.signal_processing.e2e_ppg.reconstruction.select_effective_signal_and_partitions
predicate_id: select_effective_signal_and_partitions
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(reconstruction_states: list[dict], reconstruction_flag: bool, sig_conditioned: np.ndarray, clean_indices_flat: list[int], noisy_indices_grouped: list[list[int]]) -> tuple[np.ndarray, list[int], list[list[int]]]"
informal_desc: Choose the last successful reconstruction state when available; otherwise keep the conditioned signal and current partitions.
conceptual_summary: ""
inputs:
  - name: reconstruction_states
    type_desc: list[dict]
    constraints: Ordered successful reconstruction updates
  - name: reconstruction_flag
    type_desc: bool
    constraints: Indicates whether a valid reconstruction exists
  - name: sig_conditioned
    type_desc: np.ndarray
    constraints: Fallback signal when reconstruction fails
  - name: clean_indices_flat
    type_desc: list[int]
    constraints: Fallback clean indices in flat form
  - name: noisy_indices_grouped
    type_desc: list[list[int]]
    constraints: Fallback noisy partitions
outputs:
  - name: ppg_working
    type_desc: np.ndarray
    constraints: Selected working-rate signal
  - name: clean_indices_flat_effective
    type_desc: list[int]
    constraints: Effective clean indices at working rate
  - name: noisy_indices_grouped_effective
    type_desc: list[list[int]]
    constraints: Effective noisy partitions at working rate
concept_type: greedy
verification_level: type_checked
used_by_cdgs: []
references:
  - title: wang2022ppg
    citation: Exact E2E-PPG reconstruction attribution.
  - title: feli2023pipeline
    citation: Primary E2E-PPG pipeline paper.
  - title: repo_e2e_ppg
    citation: Upstream implementation is mapped to the vendored E2E-PPG repository.
title: Select_effective_signal_and_partitions
description: Choose the last successful reconstruction state when available; otherwise keep the conditioned signal and current partitions.
---

# Select_effective_signal_and_partitions

> Choose the last successful reconstruction state when available; otherwise keep the conditioned signal and current partitions.

## Conceptual Summary



## Type Signature

```
(reconstruction_states: list[dict], reconstruction_flag: bool, sig_conditioned: np.ndarray, clean_indices_flat: list[int], noisy_indices_grouped: list[list[int]]) -> tuple[np.ndarray, list[int], list[list[int]]]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
