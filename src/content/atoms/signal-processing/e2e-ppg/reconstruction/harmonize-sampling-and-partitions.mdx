---
fqdn: sciona.atoms.signal_processing.e2e_ppg.reconstruction.harmonize_sampling_and_partitions
predicate_id: harmonize_sampling_and_partitions
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(sig: np.ndarray, clean_indices: list[list[int]], noisy_indices: list[list[int]], sampling_rate: int, model_sampling_rate: int) -> tuple[np.ndarray, int, bool, int|null, float|null, list[int], list[list[int]]]"
informal_desc: Convert signal and index partitions to the model sampling frequency when needed; otherwise normalize clean indices to a flat representation.
conceptual_summary: ""
inputs:
  - name: sig
    type_desc: np.ndarray
    constraints: 1-D signal
  - name: clean_indices
    type_desc: list[list[int]]
    constraints: Grouped clean index ranges
  - name: noisy_indices
    type_desc: list[list[int]]
    constraints: Grouped noisy index ranges
  - name: sampling_rate
    type_desc: int
    constraints: Input signal sampling frequency
  - name: model_sampling_rate
    type_desc: int
    constraints: Target reconstruction sampling frequency
outputs:
  - name: sig_working
    type_desc: np.ndarray
    constraints: Signal at working sampling rate
  - name: sampling_rate_working
    type_desc: int
    constraints: Equals model_sampling_rate after conversion
  - name: resampling_flag
    type_desc: bool
    constraints: True iff input was resampled
  - name: sampling_rate_original
    type_desc: Optional[int]
    constraints: Set only when resampling_flag is True
  - name: resampling_rate
    type_desc: Optional[float]
    constraints: Original/model rate ratio when resampled
  - name: clean_indices_flat
    type_desc: list[int]
    constraints: Flattened clean indices at working rate
  - name: noisy_indices_grouped
    type_desc: list[list[int]]
    constraints: Grouped noisy indices at working rate
concept_type: sampler
verification_level: type_checked
used_by_cdgs: []
references:
  - title: wang2022ppg
    citation: Exact E2E-PPG reconstruction attribution.
  - title: feli2023pipeline
    citation: Primary E2E-PPG pipeline paper.
  - title: repo_e2e_ppg
    citation: Upstream implementation is mapped to the vendored E2E-PPG repository.
title: Harmonize_sampling_and_partitions
description: Convert signal and index partitions to the model sampling frequency when needed; otherwise normalize clean indices to a flat representation.
---

# Harmonize_sampling_and_partitions

> Convert signal and index partitions to the model sampling frequency when needed; otherwise normalize clean indices to a flat representation.

## Conceptual Summary



## Type Signature

```
(sig: np.ndarray, clean_indices: list[list[int]], noisy_indices: list[list[int]], sampling_rate: int, model_sampling_rate: int) -> tuple[np.ndarray, int, bool, int|null, float|null, list[int], list[list[int]]]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
