---
fqdn: sciona.atoms.signal_processing.e2e_ppg.reconstruction.update_iteration_state_from_stitched_signal
predicate_id: update_iteration_state_from_stitched_signal
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(stitched_signal_native: np.ndarray, clean_len: int, len_rec: int, remaining_noise_current: int, reconstructed_noise_current: list[float]) -> tuple[list[float], int, np.ndarray, int]"
informal_desc: Extract next reconstructed chunk, update remaining target, append accumulation, and slide clean context window.
conceptual_summary: ""
inputs:
  - name: stitched_signal_native
    type_desc: np.ndarray
    constraints: native-length stitched signal
  - name: clean_len
    type_desc: int
    constraints: "> 0"
  - name: len_rec
    type_desc: int
    constraints: "> 0"
  - name: remaining_noise_current
    type_desc: int
    constraints: ">= 0"
  - name: reconstructed_noise_current
    type_desc: list[float]
    constraints: accumulator before this iteration
outputs:
  - name: reconstructed_noise_next
    type_desc: list[float]
    constraints: accumulator after append
  - name: remaining_noise_next
    type_desc: int
    constraints: decremented by min(len_rec, remaining_noise_current)
  - name: ppg_clean_next
    type_desc: np.ndarray
    constraints: shifted context window for next iteration
  - name: current_reconstructed_len_next
    type_desc: int
    constraints: len(reconstructed_noise_next)
concept_type: data_assembly
verification_level: type_checked
used_by_cdgs: []
references:
  - title: wang2022ppg
    citation: Exact E2E-PPG reconstruction attribution.
  - title: feli2023pipeline
    citation: Primary E2E-PPG pipeline paper.
  - title: repo_e2e_ppg
    citation: Upstream implementation is mapped to the vendored E2E-PPG repository.
title: Update_iteration_state_from_stitched_signal
description: Extract next reconstructed chunk, update remaining target, append accumulation, and slide clean context window.
---

# Update_iteration_state_from_stitched_signal

> Extract next reconstructed chunk, update remaining target, append accumulation, and slide clean context window.

## Conceptual Summary



## Type Signature

```
(stitched_signal_native: np.ndarray, clean_len: int, len_rec: int, remaining_noise_current: int, reconstructed_noise_current: list[float]) -> tuple[list[float], int, np.ndarray, int]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
