---
fqdn: sciona.atoms.signal_processing.e2e_ppg.reconstruction.initialize_reconstruction_state
predicate_id: initialize_reconstruction_state
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(ppg_clean: np.ndarray, noise: list, sampling_rate: int, shifting_step_seconds: int|float, upsampling_rate: float|int) -> tuple[np.ndarray, int, int, list[float], int, int, int, float|int]"
informal_desc: Derive fixed reconstruction window settings and initialize iterative state from inputs.
conceptual_summary: ""
inputs:
  - name: ppg_clean
    type_desc: np.ndarray
    constraints: 1D clean context segment
  - name: noise
    type_desc: list
    constraints: target noise sequence; only its length is used
  - name: sampling_rate
    type_desc: int
    constraints: "> 0"
  - name: shifting_step_seconds
    type_desc: float
    constraints: constant 5 in current implementation
  - name: upsampling_rate
    type_desc: float
    constraints: external constant (UPSAMPLING_RATE)
outputs:
  - name: ppg_clean_current
    type_desc: np.ndarray
    constraints: initial loop context
  - name: target_noise_len
    type_desc: int
    constraints: len(noise)
  - name: len_rec
    type_desc: int
    constraints: int(shifting_step_seconds * sampling_rate)
  - name: reconstructed_noise_current
    type_desc: list[float]
    constraints: initialized empty
  - name: remaining_noise_current
    type_desc: int
    constraints: initialized to target_noise_len
  - name: current_reconstructed_len
    type_desc: int
    constraints: initialized to 0
  - name: sampling_rate_state
    type_desc: int
    constraints: pass-through
  - name: upsampling_rate_state
    type_desc: float
    constraints: pass-through
concept_type: arithmetic
verification_level: type_checked
used_by_cdgs: []
references:
  - title: wang2022ppg
    citation: Exact E2E-PPG reconstruction attribution.
  - title: feli2023pipeline
    citation: Primary E2E-PPG pipeline paper.
  - title: repo_e2e_ppg
    citation: Upstream implementation is mapped to the vendored E2E-PPG repository.
title: Initialize_reconstruction_state
description: Derive fixed reconstruction window settings and initialize iterative state from inputs.
---

# Initialize_reconstruction_state

> Derive fixed reconstruction window settings and initialize iterative state from inputs.

## Conceptual Summary



## Type Signature

```
(ppg_clean: np.ndarray, noise: list, sampling_rate: int, shifting_step_seconds: int|float, upsampling_rate: float|int) -> tuple[np.ndarray, int, int, list[float], int, int, int, float|int]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
