---
fqdn: sciona.atoms.signal_processing.biosppy.svm_proc.get_id_rates
predicate_id: get_id_rates
repo: sciona-atoms-signal
domain: signal_processing
type_signature: "(H: Any, M: Any, R: Any, N: Any, thresholds: Any) -> Any"
informal_desc: |-
  Compute identification rates from the confusion matrix.

  Parameters
  ----------
  H : array
      Hit counts for each classifier threshold.
  M : array
      Miss counts for each classifier threshold.
  R : array
      Reject counts for each classifier threshold.
  N : int
      Number of test samples.
  thresholds : array
      Classifier thresholds.

  Returns
  -------
  Acc : array
      Accuracy at each classifier threshold.
  Err : array
      Error rate at each classifier threshold.
  MR : array
      Miss Rate at each classifier threshold.
  RR : array
      Reject Rate at each classifier threshold.
  EID : array
      Error of Identification points, with format (threshold, rate).
  EER : array
      Equal Error Rate points, with format (threshold, rate).
conceptual_summary: ""
inputs:
  - name: H
    type_desc: NDArray[np.float64]
    constraints: valid NDArray[np.float64]
    required: true
    default_value_repr: ""
  - name: M
    type_desc: NDArray[np.float64]
    constraints: valid NDArray[np.float64]
    required: true
    default_value_repr: ""
  - name: R
    type_desc: NDArray[np.float64]
    constraints: valid NDArray[np.float64]
    required: true
    default_value_repr: ""
  - name: N
    type_desc: int
    constraints: valid int
    required: true
    default_value_repr: ""
  - name: thresholds
    type_desc: NDArray[np.float64]
    constraints: valid NDArray[np.float64]
    required: true
    default_value_repr: ""
outputs:
  - name: result
    type_desc: dict[str, object]
    constraints: valid dict[str, object]
    required: true
    default_value_repr: ""
concept_type: analysis
verification_level: type_checked
used_by_cdgs: []
references:
  - title: repo_biosppy
    citation: Primary BioSPPy software attribution.
title: Get_id_rates
description: |-
  Compute identification rates from the confusion matrix.

  Parameters
  ----------
  H : array
      Hit counts for each classifier threshold.
  M : array
      Miss count…
---

# Get_id_rates

> Compute identification rates from the confusion matrix.

Parameters
----------
H : array
    Hit counts for each classifier threshold.
M : array
    Miss counts for each classifier threshold.
R : array
    Reject counts for each classifier threshold.
N : int
    Number of test samples.
thresholds : array
    Classifier thresholds.

Returns
-------
Acc : array
    Accuracy at each classifier threshold.
Err : array
    Error rate at each classifier threshold.
MR : array
    Miss Rate at each classifier threshold.
RR : array
    Reject Rate at each classifier threshold.
EID : array
    Error of Identification points, with format (threshold, rate).
EER : array
    Equal Error Rate points, with format (threshold, rate).

## Conceptual Summary



## Type Signature

```
(H: Any, M: Any, R: Any, N: Any, thresholds: Any) -> Any
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
