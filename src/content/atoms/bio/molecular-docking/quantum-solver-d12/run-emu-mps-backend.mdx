---
fqdn: sciona.atoms.bio.molecular_docking.quantum_solver_d12.run_emu_mps_backend
predicate_id: run_emu_mps_backend
repo: sciona-atoms-bio
domain: bio
type_signature: "(seq: pulser.Sequence, run_emu_mps: bool, dt: int, final_time: float, n_shots: int, max_bond_dim: int) -> dict[str, int]"
informal_desc: Configures and executes the EMU-MPS tensor-network simulator (bond-dim 200) with the derived time grid, then extracts raw bitstring counts at the final evaluation time.
conceptual_summary: ""
inputs: []
outputs: []
concept_type: custom
verification_level: type_checked
used_by_cdgs: []
references:
  - title: moleculardocking2025
    citation: Primary Molecular-Docking paper.
  - title: repo_molecular_docking
    citation: Upstream implementation is mapped to the vendored Molecular-Docking repository.
title: Run_emu_mps_backend
description: Configures and executes the EMU-MPS tensor-network simulator (bond-dim 200) with the derived time grid, then extracts raw bitstring counts at the final evaluat…
---

# Run_emu_mps_backend

> Configures and executes the EMU-MPS tensor-network simulator (bond-dim 200) with the derived time grid, then extracts raw bitstring counts at the final evaluation time.

## Conceptual Summary



## Type Signature

```
(seq: pulser.Sequence, run_emu_mps: bool, dt: int, final_time: float, n_shots: int, max_bond_dim: int) -> dict[str, int]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
