---
fqdn: sciona.atoms.robotics.rust_robotics.n_joint_arm_2d.compute_local_jacobian_linearization
predicate_id: compute_local_jacobian_linearization
repo: sciona-atoms-robotics
domain: robotics
type_signature: "(self: ModelD<f64>, x: DVector<f64>, u: DVector<f64>, _t: f64) -> (DMatrix<f64>, DMatrix<f64>)"
informal_desc: Evaluate local Jacobian linearization matrices for dynamics at (x, u, t). In current code this step is declared but not implemented (todo!).
conceptual_summary: ""
inputs:
  - name: self
    type_desc: object
    constraints: isinstance(x, (int, float))
  - name: x
    type_desc: np.ndarray
    constraints: ndim >= 1
  - name: u
    type_desc: np.ndarray
    constraints: ndim >= 1
  - name: _t
    type_desc: float
    constraints: isinstance(x, (int, float))
outputs:
  - name: jacobian
    type_desc: tuple[np.ndarray, np.ndarray]
    constraints: Intended output; currently unavailable due to todo!()
concept_type: analysis
verification_level: type_checked
used_by_cdgs: []
references:
  - title: repo_rust_robotics
    citation: Primary repository attribution for rust_robotics atoms.
title: Compute_local_jacobian_linearization
description: Evaluate local Jacobian linearization matrices for dynamics at (x, u, t). In current code this step is declared but not implemented (todo!).
---

# Compute_local_jacobian_linearization

> Evaluate local Jacobian linearization matrices for dynamics at (x, u, t). In current code this step is declared but not implemented (todo!).

## Conceptual Summary



## Type Signature

```
(self: ModelD<f64>, x: DVector<f64>, u: DVector<f64>, _t: f64) -> (DMatrix<f64>, DMatrix<f64>)
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
