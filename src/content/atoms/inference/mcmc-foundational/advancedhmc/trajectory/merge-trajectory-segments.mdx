---
fqdn: sciona.atoms.inference.mcmc_foundational.advancedhmc.trajectory.merge_trajectory_segments
predicate_id: merge_trajectory_segments
repo: sciona-atoms
domain: inference
type_signature: "(left_tree: BinaryTree, right_tree: BinaryTree, rng: AbstractRNG, sampler_after_initial_build: AbstractTrajectorySampler, sampler_after_expansion: AbstractTrajectorySampler) -> tuple[BinaryTree, AbstractTrajectorySampler]"
informal_desc: Combines the left and right trajectory subtrees into a single, larger binary tree and merges their corresponding samplers.
conceptual_summary: ""
inputs:
  - name: left_tree
    type_desc: BinaryTree
    constraints: x is not None
  - name: right_tree
    type_desc: BinaryTree
    constraints: x is not None
  - name: rng
    type_desc: AbstractRNG
    constraints: isinstance(x, str)
  - name: sampler_after_initial_build
    type_desc: AbstractTrajectorySampler
    constraints: isinstance(x, str)
  - name: sampler_after_expansion
    type_desc: AbstractTrajectorySampler
    constraints: isinstance(x, str)
outputs:
  - name: combined_tree
    type_desc: BinaryTree
    constraints: x is not None
  - name: combined_sampler
    type_desc: AbstractTrajectorySampler
    constraints: isinstance(x, str)
concept_type: combinatorics
verification_level: type_checked
used_by_cdgs: []
references:
  - title: hoffman2014nuts
    citation: Canonical NUTS reference for doubling trajectory trees and No-U-Turn termination.
  - title: advancedhmc2020
    citation: Package paper for AdvancedHMC.jl trajectory construction.
  - title: repo_advancedhmc_jl
    citation: Repository-family attribution; this wrapper returns compact NumPy leaves rather than Julia BinaryTree objects.
  - title: neal2011hmc
    citation: Canonical HMC reference for Hamiltonian dynamics and Metropolis-style energy diagnostics used by NUTS.
title: Merge_trajectory_segments
description: Combines the left and right trajectory subtrees into a single, larger binary tree and merges their corresponding samplers.
---

# Merge_trajectory_segments

> Combines the left and right trajectory subtrees into a single, larger binary tree and merges their corresponding samplers.

## Conceptual Summary



## Type Signature

```
(left_tree: BinaryTree, right_tree: BinaryTree, rng: AbstractRNG, sampler_after_initial_build: AbstractTrajectorySampler, sampler_after_expansion: AbstractTrajectorySampler) -> tuple[BinaryTree, AbstractTrajectorySampler]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
