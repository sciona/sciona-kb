---
fqdn: sciona.atoms.inference.mcmc_foundational.advancedhmc.trajectory.build_initial_trajectory_segment
predicate_id: build_initial_trajectory_segment
repo: sciona-atoms
domain: inference
type_signature: "(rng: AbstractRNG, trajectory_params: Trajectory, hamiltonian: Hamiltonian, start_state: PhasePoint, sampler: AbstractTrajectorySampler, direction: Int, depth: Int, initial_energy: AbstractFloat) -> tuple[BinaryTree, AbstractTrajectorySampler, Termination]"
informal_desc: Recursively constructs the first half of the trajectory tree for the current depth `j`. This is the first of two recursive calls that implement the doubling mechanism.
conceptual_summary: ""
inputs:
  - name: rng
    type_desc: AbstractRNG
    constraints: isinstance(x, str)
  - name: trajectory_params
    type_desc: Trajectory
    constraints: x is not None
  - name: hamiltonian
    type_desc: Hamiltonian
    constraints: x is not None
  - name: start_state
    type_desc: PhasePoint
    constraints: isinstance(x, int)
  - name: sampler
    type_desc: AbstractTrajectorySampler
    constraints: isinstance(x, str)
  - name: direction
    type_desc: Int
    constraints: Value is -1 or 1
  - name: depth
    type_desc: Int
    constraints: Represents j-1 from the original code
  - name: initial_energy
    type_desc: AbstractFloat
    constraints: isinstance(x, (int, float))
outputs:
  - name: initial_tree
    type_desc: BinaryTree
    constraints: x is not None
  - name: sampler_after_initial_build
    type_desc: AbstractTrajectorySampler
    constraints: isinstance(x, str)
  - name: termination_after_initial_build
    type_desc: Termination
    constraints: x is not None
concept_type: divide_and_conquer
verification_level: type_checked
used_by_cdgs: []
references:
  - title: hoffman2014nuts
    citation: Canonical NUTS reference for doubling trajectory trees and No-U-Turn termination.
  - title: advancedhmc2020
    citation: Package paper for AdvancedHMC.jl trajectory construction.
  - title: repo_advancedhmc_jl
    citation: Repository-family attribution; this wrapper returns compact NumPy leaves rather than Julia BinaryTree objects.
  - title: neal2011hmc
    citation: Canonical HMC reference for Hamiltonian dynamics and Metropolis-style energy diagnostics used by NUTS.
title: Build_initial_trajectory_segment
description: Recursively constructs the first half of the trajectory tree for the current depth `j`. This is the first of two recursive calls that implement the doubling me…
---

# Build_initial_trajectory_segment

> Recursively constructs the first half of the trajectory tree for the current depth `j`. This is the first of two recursive calls that implement the doubling mechanism.

## Conceptual Summary



## Type Signature

```
(rng: AbstractRNG, trajectory_params: Trajectory, hamiltonian: Hamiltonian, start_state: PhasePoint, sampler: AbstractTrajectorySampler, direction: Int, depth: Int, initial_energy: AbstractFloat) -> tuple[BinaryTree, AbstractTrajectorySampler, Termination]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
