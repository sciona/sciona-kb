---
fqdn: sciona.atoms.inference.mcmc_foundational.advancedhmc.integrator.commitcandidateanddecidecontinuation
predicate_id: commitcandidateanddecidecontinuation
repo: sciona-atoms
domain: inference
type_signature: "(candidate_state: {i, theta_next, r_next, value_next, gradient_next, z_candidate}, trajectory_accum: Nothing | Vector{P}, collect_full_trajectory: Bool, step_count: Int) -> tuple[{i, theta, r, value, gradient, z_current}, Nothing | Vector{P}, Bool, Bool]"
informal_desc: Optionally append candidate to trajectory, evaluate finiteness, truncate realized trajectory on first invalid sample, and compute next loop state plus continuation decision.
conceptual_summary: ""
inputs:
  - name: candidate_state
    type_desc: object
    constraints: x is not None
  - name: trajectory_accum
    type_desc: Optional[np.ndarray]
    constraints: current accumulation buffer
  - name: collect_full_trajectory
    type_desc: Bool
    constraints: isinstance(x, bool)
  - name: step_count
    type_desc: Int
    constraints: loop upper bound
outputs:
  - name: loop_state_next
    type_desc: object
    constraints: if continue, i increments; otherwise holds terminal candidate state
  - name: trajectory_accum_next
    type_desc: Optional[np.ndarray]
    constraints: contains accepted prefix; truncated to realized length on invalid candidate
  - name: continue_loop
    type_desc: Bool
    constraints: true iff candidate is finite and i < step_count
  - name: is_valid
    type_desc: Bool
    constraints: finiteness of terminal candidate
concept_type: analysis
verification_level: type_checked
used_by_cdgs: []
references:
  - title: neal2011hmc
    citation: Canonical HMC reference for leapfrog Hamiltonian phase-point updates.
  - title: advancedhmc2020
    citation: Package paper for the AdvancedHMC.jl integrator family that this NumPy atom mirrors at a compact phase-point level.
  - title: repo_advancedhmc_jl
    citation: Repository-family attribution; this wrapper is a local NumPy implementation rather than a direct Julia object binding.
title: Commitcandidateanddecidecontinuation
description: Optionally append candidate to trajectory, evaluate finiteness, truncate realized trajectory on first invalid sample, and compute next loop state plus continua…
---

# Commitcandidateanddecidecontinuation

> Optionally append candidate to trajectory, evaluate finiteness, truncate realized trajectory on first invalid sample, and compute next loop state plus continuation decision.

## Conceptual Summary



## Type Signature

```
(candidate_state: {i, theta_next, r_next, value_next, gradient_next, z_candidate}, trajectory_accum: Nothing | Vector{P}, collect_full_trajectory: Bool, step_count: Int) -> tuple[{i, theta, r, value, gradient, z_current}, Nothing | Vector{P}, Bool, Bool]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
