---
fqdn: sciona.atoms.inference.mcmc_foundational.advancedhmc.integrator.normalizeintegrationcontrols
predicate_id: normalizeintegrationcontrols
repo: sciona-atoms
domain: inference
type_signature: "(lf: DefaultLeapfrog context, n_steps: Int, fwd: Bool, full_trajectory: Val{Bool}) -> tuple[Int, Step-size scalar/vector compatible with theta and r, Bool]"
informal_desc: "Convert integration controls into canonical form: absolute step count, signed step size based on direction, and trajectory collection mode."
conceptual_summary: ""
inputs:
  - name: lf
    type_desc: object
    constraints: must provide step_size(lf)
  - name: n_steps
    type_desc: Int
    constraints: can be negative; magnitude determines number of iterations
  - name: fwd
    type_desc: Bool
    constraints: true for forward integration, false for backward
  - name: full_trajectory
    type_desc: bool
    constraints: compile-time flag
outputs:
  - name: step_count
    type_desc: Int
    constraints: step_count = abs(n_steps), step_count >= 0
  - name: epsilon
    type_desc: float
    constraints: "epsilon = (fwd ? step_size(lf) : -step_size(lf))'"
  - name: collect_full_trajectory
    type_desc: Bool
    constraints: true iff full_trajectory is Val(true)
concept_type: arithmetic
verification_level: type_checked
used_by_cdgs: []
references:
  - title: neal2011hmc
    citation: Canonical HMC reference for leapfrog Hamiltonian phase-point updates.
  - title: advancedhmc2020
    citation: Package paper for the AdvancedHMC.jl integrator family that this NumPy atom mirrors at a compact phase-point level.
  - title: repo_advancedhmc_jl
    citation: Repository-family attribution; this wrapper is a local NumPy implementation rather than a direct Julia object binding.
title: Normalizeintegrationcontrols
description: "Convert integration controls into canonical form: absolute step count, signed step size based on direction, and trajectory collection mode."
---

# Normalizeintegrationcontrols

> Convert integration controls into canonical form: absolute step count, signed step size based on direction, and trajectory collection mode.

## Conceptual Summary



## Type Signature

```
(lf: DefaultLeapfrog context, n_steps: Int, fwd: Bool, full_trajectory: Val{Bool}) -> tuple[Int, Step-size scalar/vector compatible with theta and r, Bool]
```

## Deep Dive

{/* Community contributions welcome -- click "Edit this page on GitHub" above. */}
